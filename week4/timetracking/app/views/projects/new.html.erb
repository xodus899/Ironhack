<h2> Create a new project </h2>

<%= form_for @project do |f|%>
	<%= f.label :name %> 
	<%= f.text_field :name %> 

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.button %>
<% end %>

