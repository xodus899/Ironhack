<h2> Create a new user </h2>

<%= form_for @newuser do |the_user|%>

	<%= the_user.label :name %> 
	<%= the_user.text_field :name %> 

	<%= the_user.label :email %>
	<%= the_user.text_field :email %>

	<%= the_user.submit %>
<% end %>